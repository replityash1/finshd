/**
 * js/config.js — Exam definitions, cross-exam overlap map, color tokens
 * 
 * Built in Phase 1. Contains:
 * - EXAMS: array of { id, name, accent } for all tracked exams
 * - CROSS_EXAM_ID_MAP: maps shared topic IDs across exams (with overlap-stats fix)
 * - Exam-to-accent-color mapping
 */
