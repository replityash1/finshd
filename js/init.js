/**
 * js/init.js — Bootstrap: auth listener, data load, initial render
 * 
 * Built in Phase 2. Entry point that wires everything together on DOMContentLoaded.
 * Loaded last in index.html (depends on all other modules).
 */
