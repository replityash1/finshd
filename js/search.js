/**
 * js/search.js — Topic search + command palette
 * 
 * Built in Phase 3. Builds a flattened in-memory index per exam on load.
 * Cmd/Ctrl+K or "/" opens the palette; Esc closes; arrow keys navigate.
 */
