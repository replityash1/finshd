/**
 * js/state.js — Single mutable app state object + accessors
 * 
 * Built in Phase 1. The single source of truth for all runtime state.
 * Other modules read/write through this, not through scattered globals.
 * See ARCHITECTURE.md §3 for the full state shape.
 */
