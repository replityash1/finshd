/**
 * js/utils.js — escapeHTML, staleness/date helpers, DOM helpers
 * 
 * Built in Phase 1. Shared utility functions used across all views.
 * escapeHTML() escapes & < > " ' — the five mandatory characters.
 */
